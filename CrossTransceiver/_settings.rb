module CrossTransceiverSettings
    # controls how often the portraits blink (if applicable)
    # must be a number between 0 (never blink) and 65536 (blinking constantly)
    BLINK_RATE = 768

    # FPS for blink animation
    BLINK_FRAME_RATE = 5.0

    # FPS for mouth animation
    MOUTH_RAME_RATE = 5.0
end